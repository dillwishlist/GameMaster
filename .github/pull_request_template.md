## What changed

<!-- One or two sentences. Link the issue or the content file if there is one. -->

## Rehearsed end-to-end?

The bar is that it works live, in one shot, in a room full of people.

- [ ] Ran `npm start` and drove the change through the **host view and the
      display view together** (tablet or laptop host + the TV output)
- [ ] Not rehearsed — here is why that is acceptable for this change:

## Invariant check

- [ ] No-device play still works: the change needs nothing but the host and the TV
- [ ] The host can still fix it by hand (manual score override is unaffected)
- [ ] No unrevealed answer can reach the display projection

## Checks

- [ ] `npm run check` passes locally (typecheck, tests, smoke)

## Anything a host needs to know

<!-- New content fields, a changed key on the host view, a step for docs/RUNDAY.md.
     Write "none" if none. Do not paste passphrases, tokens or machine addresses. -->
